

<div style="padding-top: 4em;">
    <div class="col-md-4 mx-auto">
        <div class="card">
            <div class="card-header">
            <h3>Editar Actividad {{activity.title}}</h3>
            </div>
            <div class="card-body">
            <form action="/activities/edit-activity/{{activity._id}}?_method=PUT" method="POST">
                <input type="hidden" name="_method" value="PUT">
                <div class="form-group">
                <input type="text" name="title" class="form-control" placeholder="Título"  value="{{activity.title}}" autofocus>
                </div>
                <div class="form-group">
                <input type="url" name="link" class="form-control" placeholder="Link" value="{{activity.link}}">
                </div>
                <div class="form-group">
                <button class="btn btn-primary btn-block" type="submit">
                    Guardar
                </button>
                </div>
            </form>
            </div>
        </div>
    </div>
</div>
