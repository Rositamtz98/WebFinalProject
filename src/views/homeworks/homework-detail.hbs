{{>sidebar}}
<div class="row">
  <div class="card">
                <div class="card-body">
                    <h2 class="card-title d-flex justify-content-between align-items-center">
                    <h2>hola<h2>


<button type="button" class="btn btn-link" onclick=" window.open('http://google.com','_blank')" type="button">
         link a la tarea</button>
                     <form action="/grades/new-homework" method="POST">
                <div class="form-group">
                    <input type="text" name="grade" class="form-control" placeholder="Grade">
                </div>
</form>
                        <a href="/homeworks/view/{{id}}" class="btn btn-success btn-block">Grade homework</a>
                </div>
            </div>
    {{!-- {{#each homework}}
        <div class="col-md-3">
            <div class="card">
                <div class="card-body">
                    <h2 class="card-title d-flex justify-content-between align-items-center">
                        {{homework_id}} <a href="/homeworks/edit/{{_id}}"><i class="fas fa-edit"></i></a>
                    </h4>
                    <h2>{{link}}</h2>
                    <h4>{{subject}}</h2>
                    <h4>{{teacher_id}}</h4>
                    <img src={{img}}>
                        <a href="/homeworks/view/{{id}}" class="btn btn-success btn-block">Ver detalle</a>
                    <form action="/homeworks/delete/{{_id}}?_method=DELETE" method="POST">
                        <input type="hidden" name="_method" value="DELETE">
                        <button class="btn btn-danger btn-block btn-sm" type="submit">
                            Borrar
                        </button>
                    </form>
                </div>
            </div>
        </div>
    {{else}}
        <div class="card mx-auto">
            <div class="card-body">
                <p class="lead">No hay tareas para este curso</p>
                <a href="/homeworks/add" class="btn btn-success btn-block">Crea una</a>
            </div>
        </div>
    {{/each}} --}}
</div>
